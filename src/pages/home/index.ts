import { lazy } from 'react';

export const HomePage = lazy(() => import('./HomePage'));
export const PostInfoPage = lazy(() => import('./PostInfoPage'));
export const PostPinPage = lazy(() => import('./PostPinPage'));
